# Getting-info-about-user-s-location
This will guide to get the two useful information about the user's location(longitude and latitude), so that we can use that to find the exact location in google map.
